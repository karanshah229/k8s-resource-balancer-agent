# TODO(candidate): Write the USER prompt that kicks off a single run.
# Inputs: namespace (`{namespace}`) and Slack channel (`{slack_channel}`) placeholders.
# Output: step-by-step runtime instructions that reference the system rules and include the final Slack handoff.
