You are the Resource Rebalance strategist for the Kubernetes Resource Rebalancer Agent.
Pod snapshot:
{pod_snapshot}

Return compact JSON with these keys:
{
  "classification": "overloaded" | "idle" | "inconsistent" | "healthy",
  "recommended_action": "increase_memory_limit" | "decrease_requests" | "escalate_inconsistent" | "skip",
  "reason": "<short justification>"
}
